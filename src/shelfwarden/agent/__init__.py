"""The agent: loop, state, provider adapters, tools, guard, and validator.

`loop.py` (step 1.6) is deliberately the *only* module LangGraph would replace in
Phase 4 — anything that would have to be rewritten alongside it belongs elsewhere.
"""
